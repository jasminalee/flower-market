# 鲜花市场后端系统 - 开发进度报告

## ✅ 已完成功能（阶段一）

### 1. 数据库层
- ✅ 完整的数据库 Schema（schema.sql）
  - 17张核心业务表
  - utf8mb4 编码
  - 完整的索引和外键约束
  - 初始化数据

### 2. 实体层（Entity）
- ✅ 16个实体类，对应所有数据库表
- ✅ 使用 Lombok 简化代码
- ✅ MyBatis-Plus 注解完整

### 3. 数据访问层（Mapper）
- ✅ 16个 Mapper 接口
- ✅ 继承 MyBatis-Plus BaseMapper
- ✅ 自动扫描配置

### 4. 公共组件
- ✅ Result 统一响应类
- ✅ Constants 业务常量类
- ✅ BusinessException 业务异常类
- ✅ GlobalExceptionHandler 全局异常处理器

### 5. 业务逻辑层（Service）
- ✅ CustomerService - 顾客注册/登录
- ✅ MerchantService - 商家注册/登录
- ✅ ProductService - 产品CRUD、分页查询
- ✅ ProductCategoryService - 分类查询

### 6. 接口层（Controller）
- ✅ CustomerController - 顾客管理接口
- ✅ MerchantController - 商家管理接口
- ✅ ProductController - 产品管理接口
- ✅ ProductCategoryController - 分类查询接口

### 7. 数据传输对象（/VO）
- ✅ CustomerRegister、Customer
- ✅ MerchantRegister、Merchant
- ✅ Product
- ✅ CustomerVO、MerchantVO、ProductVO

### 8. API 文档
- ✅ Knife4j 配置完成
- ✅ Swagger 注解完整

## 🔐 安全特性

### 密码加密
- 使用 Hutool 的 MD5 加密
- 注册时自动加密：`SecureUtil.md5(password)`
- 登录时密文比对

### 业务验证
- 邮箱/手机号唯一性检查
- 参数校验注解（@NotBlank, @Email, @Pattern）
- 商家状态检查（待审核/已拒绝/已暂停不能登录）

## 📋 接口清单

### 顾客接口（/api/customer）
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /register | 顾客注册 |
| POST | /login | 顾客登录 |
| GET | /profile/{userId} | 获取个人信息 |

### 商家接口（/api/merchant）
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /register | 商家注册（状态为待审核） |
| POST | /login | 商家登录（需审核通过） |
| GET | /profile/{merchId} | 获取商家信息 |

### 产品接口（/api/products）
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | / | 创建产品 |
| PUT | /{id} | 更新产品 |
| DELETE | /{id} | 删除产品（逻辑删除） |
| GET | /{id} | 获取产品详情 |
| GET | / | 分页查询产品列表 |
| PUT | /{id}/publish | 上架产品 |
| PUT | /{id}/unpublish | 下架产品 |

### 分类接口（/api/products/categories）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | / | 获取所有分类 |
| GET | /parent/{parentId} | 获取子分类 |
| GET | /{id} | 获取分类详情 |

## 🚀 如何启动

### 1. 初始化数据库
```bash
# 连接MySQL
mysql -u root -p

# 执行初始化脚本
source src/main/resources/schema.sql
```

### 2. 配置数据库连接
编辑 `src/main/resources/application.yml`：
```yaml
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/flower_market?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
```

### 3. 启动项目
```bash
mvn spring-boot:run
```

### 4. 访问API文档
启动成功后访问：
- **Knife4j文档**: http://localhost:18091/doc.html
- **Swagger UI**: http://localhost:18091/swagger-ui.html

## 📝 测试示例

### 注册顾客
```bash
curl -X POST http://localhost:18091/api/customer/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "张三",
    "email": "zhangsan@example.com",
    "phone": "13800138000",
    "password": "123456",
    "gender": "男",
    "address": "北京市朝阳区"
  }'
```

### 顾客登录
```bash
curl -X POST http://localhost:18091/api/customer/login \
  -H "Content-Type: application/json" \
  -d '{
    "account": "zhangsan@example.com",
    "password": "123456"
  }'
```

### 创建产品
```bash
curl -X POST http://localhost:18091/api/products \
  -H "Content-Type: application/json" \
  -d '{
    "merchId": 1,
    "catId": 3,
    "name": "红玫瑰花束",
    "price": 99.00,
    "stock": 100,
    "mainImage": "/images/rose.jpg",
    "description": "新鲜红玫瑰，浪漫之选"
  }'
```

### 分页查询产品
```bash
curl "http://localhost:18091/api/products?current=1&size=10&keyword=玫瑰"
```

## 📊 项目结构
```
src/main/java/vtc/xueqing/flower/
├── common/              # 公共类
│   ├── Constants.java   # 常量定义
│   └── Result.java      # 统一响应
├── config/              # 配置类
│   └── Knife4jConfig.java
├── controller/          # 控制器层
│   ├── CustomerController.java
│   ├── MerchantController.java
│   ├── ProductController.java
│   └── ProductCategoryController.java
├── dto/                 # 数据传输对象
│   ├── customer/
│   ├── merchant/
│   └── product/
├── entity/              # 实体类（16个）
├── exception/           # 异常处理
│   ├── BusinessException.java
│   └── GlobalExceptionHandler.java
├── mapper/              # Mapper接口（16个）
├── service/             # 服务接口
│   └── impl/            # 服务实现
└── vo/                  # 视图对象
    ├── CustomerVO.java
    ├── MerchantVO.java
    └── ProductVO.java
```

## 🎯 下一步开发计划

### 阶段二：核心业务（待开发）
- [ ] 购物车功能
- [ ] 订单管理功能
- [ ] 支付功能（模拟）
- [ ] 优惠券功能

### 阶段三：增值功能（待开发）
- [ ] 产品收藏与评价
- [ ] 签到功能
- [ ] 养护知识
- [ ] 产品溯源

### 阶段四：管理功能（待开发）
- [ ] 商家管理后台
- [ ] 管理员后台
- [ ] 数据统计与报表

## 📌 技术亮点

1. **标准三层架构**: Controller → Service → Mapper
2. **统一异常处理**: 全局捕获业务异常和系统异常
3. **参数校验**: 使用 Validation 注解自动校验
4. **密码安全**: MD5 加密存储
5. **API 文档**: Knife4j 自动生成交互式文档
6. **分页查询**: 使用 MyBatis-Plus 分页插件
7. **逻辑删除**: 产品删除不物理删除，修改状态
8. **状态管理**: 商家审核、产品上下架等状态流转

---

**当前版本**: 1.0.0  
**完成日期**: 2025-12-13  
**完成度**: 阶段一 100% ✅
